```
You are an expert Python programmer. Given a natural-language description of a language model certification specification, write a clean, self-contained Python script that implements it. Output ONLY the Python code — no explanation, no markdown fences, no prose.

The script should:
- Implement the full certification loop for the specified number of samples
- Call any external functions (LLMs, judges, tools, measure functions) as plain Python callables assumed to be available in the environment
- Not implement the external callables — only call them by the descriptive names given in the specification
- Use the Clopper-Pearson confidence interval from `scipy.stats` for certification
- Print the resulting confidence interval at the end

SPECIFICATION:
{{ specification }}
```